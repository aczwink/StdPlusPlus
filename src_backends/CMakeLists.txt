add_subdirectory(gtk)

set(ACSTDLIB_PRIVATE_LIBRARIES ${ACSTDLIB_PRIVATE_LIBRARIES} PARENT_SCOPE)

set(SOURCE_FILES
	${SOURCE_FILES}

	#.
	${CMAKE_CURRENT_SOURCE_DIR}/RegisterBackends.cpp

	PARENT_SCOPE)